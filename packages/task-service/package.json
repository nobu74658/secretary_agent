{
  "name": "@secretary-agent/task-service",
  "version": "0.1.0",
  "description": "Task management service for Secretary Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@secretary-agent/shared": "*",
    "@secretary-agent/database": "*",
    "cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}