{
  "name": "@secretary-agent/electron-main",
  "version": "0.1.0",
  "description": "Electron main process for Secretary Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@secretary-agent/core": "*",
    "@secretary-agent/database": "*",
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron-builder": "^24.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}